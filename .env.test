# BatchSQL 集成测试统一配置
# 所有数据库测试使用相同的参数，确保公平对比

# 测试时长配置
TEST_DURATION=300s

# 并发配置
CONCURRENT_WORKERS=100
RECORDS_PER_WORKER=20000

# 批处理配置
BATCH_SIZE=200
BUFFER_SIZE=5000
FLUSH_INTERVAL=100ms

# 数据库连接配置 (由各docker-compose文件单独设置)
# MYSQL_DSN - 在 docker-compose.mysql.yml 中设置
# POSTGRES_DSN - 在 docker-compose.postgres.yml 中设置
# REDIS_DSN - 在 docker-compose.redis.yml 中设置
