# BatchSQL SQLite 保守配置
# 基于性能分析报告优化，确保数据一致性

# 测试时长配置 - 缩短测试时间
TEST_DURATION=120s

# 并发配置 - 降低并发压力
CONCURRENT_WORKERS=2          # 降低到 2 个工作者，避免锁竞争
RECORDS_PER_WORKER=5000       # 减少记录数，确保稳定性
# 总记录数: 2 * 5000 = 10,000 条

# 批处理配置 - 保守设置
BATCH_SIZE=100               # 适中的批次大小
BUFFER_SIZE=1000             # 减少缓冲区大小
FLUSH_INTERVAL=100ms         # 稍微增加刷新间隔

# SQLite 保守性能目标:
# - RPS: 20,000 - 40,000 (稳定可达)
# - 数据一致性: 100% (优先级最高)
# - 并发: 低并发，避免锁竞争
# - 重点: 稳定性 > 性能

# 数据库连接配置
# SQLITE_DSN - 在 docker-compose.sqlite.yml 中设置